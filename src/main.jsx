import React from "react";
import ReactDOM from "react-dom/client";
import SafeToSpend from "./SafeToSpend.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SafeToSpend />
  </React.StrictMode>
);
